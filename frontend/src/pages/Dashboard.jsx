import { useEffect, useState } from 'react';import { api } from '../services/api';
export default function Dashboard(){const user=JSON.parse(localStorage.getItem('session')); const [p,setP]=useState(null); useEffect(()=>{if(user) api.get(`/portfolio/${user.id}`).then(r=>setP(r.data));},[]); if(!user) return <div>Please login.</div>; return <div className='card'><h2>{user.role} Dashboard</h2><p>{user.username} | Team {user.teamNumber}</p>{p&&<><p>Balance: ${p.user.balance}</p><p>Portfolio: ${p.portfolioValue}</p><p>Net Worth: ${p.netWorth}</p><ul>{p.stocks.map(s=><li key={s.id}>{s.stockCode} {s.companyName} ${s.currentPrice}</li>)}</ul></>}</div>;}
